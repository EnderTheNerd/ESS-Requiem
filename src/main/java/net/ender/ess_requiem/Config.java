package net.ender.ess_requiem;

import java.util.List;
import java.util.Set;
import java.util.stream.Collectors;

import net.minecraft.core.registries.BuiltInRegistries;
import net.minecraft.resources.ResourceLocation;
import net.minecraft.world.item.Item;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.fml.common.EventBusSubscriber;
import net.neoforged.fml.event.config.ModConfigEvent;
import net.neoforged.neoforge.common.ModConfigSpec;

//Hey so this mod will be configurable when its finished, but thats not now, so have whatever neoforge's default config is
//@EventBusSubscriber(modid = EndersSpellsAndStuffRequiem.MOD_ID)
public class Config
{
   //broooo config
}
